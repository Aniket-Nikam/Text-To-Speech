# Configuration is intentionally split between the two applications.
# Copy server/.env.example to server/.env for Azure, Supabase and Groq secrets.
# Copy client/.env.example to client/.env for public API/Supabase settings only.
# The root .env is not read. See README.md for every required value.
